# Copy to `.env` and fill in local values.
# Never commit real secrets.

# Application
APP_ENV=development
APP_LOG_LEVEL=info

# Example service credentials (placeholders only)
DATABASE_URL=postgres://user:changeme@localhost:5432/app
API_TOKEN=example-token
